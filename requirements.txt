# Dependencias del Sistema de Automatización de Pasaportes - OPTIMIZADO
# Instalar con: pip install -r requirements.txt

# Procesamiento de imágenes (versiones compatibles y estables en Windows/Linux)
Pillow==10.3.0
opencv-python==4.9.0.80
numpy==1.26.4

# Procesamiento de datos
pandas==2.2.2

# Utilidades del sistema
psutil==5.9.8

# PyTorch se instala condicionalmente en el instalador (CPU en Windows)

# MediaPipe para detección facial (opcional)
mediapipe==0.10.14

# OPTIMIZACIÓN: Dependencias eliminadas por problemas de compatibilidad
# - rembg (causaba conflictos de memoria)
# - onnxruntime-gpu (conflictos con numpy)
# - tensorflow (conflictos con cuDNN y protobuf)
